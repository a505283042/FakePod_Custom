# FFmpeg 命令讲解：裁黑边 + 460宽 + 24帧 + MJPEG + MP3

命令：

```powershell
ffmpeg -i "C:\Users\YCB\Desktop\FTSC SURROUND AUDIO\Taylor Swift - 22 .mp4" -vf "crop=1424:1056:250:8,scale=460:-2,fps=24" -c:v mjpeg -q:v 8 -c:a libmp3lame -b:a 192k "C:\Users\YCB\Desktop\FTSC SURROUND AUDIO\Taylor Swift - 22_460_24fps_MJPEG.avi"
```

## 1. `ffmpeg`

调用 FFmpeg 程序。

## 2. `-i "……mp4"`

指定输入文件：

```text
C:\Users\YCB\Desktop\FTSC SURROUND AUDIO\Taylor Swift - 22 .mp4
```

因为路径里有空格，所以必须加双引号。

## 3. `-vf "crop=1424:1056:250:8,scale=460:-2,fps=24"`

这是整条命令里最关键的视频滤镜部分。

### 3.1 `crop=1424:1056:250:8`

格式：

```text
crop=输出宽:输出高:X起点:Y起点
```

也就是：

```text
crop=1424:1056:250:8
```

表示：

- 裁剪后的宽度：1424
- 裁剪后的高度：1056
- 从原图左边第 250 像素开始
- 从原图顶部第 8 像素开始

如果原视频是 1920×1080，那么大致就是把左右黑边和少量上下黑边裁掉。

### 3.2 `scale=460:-2`

表示把视频宽度缩放到：

```text
460 像素
```

高度由 FFmpeg 自动按比例计算。

`-2` 的好处是：FFmpeg 会自动算出一个合法的偶数高度。

你的裁剪区域比例是：

```text
1424 ÷ 1056 ≈ 1.3485
```

所以最终高度大约是：

```text
460 ÷ 1.3485 ≈ 341
```

FFmpeg 会调整成偶数，因此大约：

```text
460×342
```

### 3.3 `fps=24`

表示把输出视频统一转换成：

```text
24 帧/秒
```

如果源视频是 30fps，它会丢掉一部分帧；如果源视频是 23.976fps，则会轻微调整到严格 24fps。

## 4. `-c:v mjpeg`

指定视频编码器：

```text
MJPEG
```

也就是 Motion JPEG。

特点：

- 每一帧基本都作为独立 JPEG 图片压缩
- 解码简单
- 对 MCU、老播放器、嵌入式设备比较友好
- 快进定位简单
- 文件体积通常比 H.264 大很多

所以转换后的 AVI 比原 MP4 大，通常是正常现象。

## 5. `-q:v 8`

控制 MJPEG 视频质量。

一般规律：

```text
数字越小 = 质量越高 = 文件越大
数字越大 = 质量越低 = 文件越小
```

例如：

```text
-q:v 2   很高质量
-q:v 5   高质量
-q:v 8   中高质量
-q:v 12  中等质量
-q:v 18  较低质量
```

当前：

```text
-q:v 8
```

属于比较均衡的设置。

如果是给 460 宽的小屏幕使用，也可以尝试：

```text
-q:v 10
```

或者：

```text
-q:v 12
```

这样文件会明显缩小。

## 6. `-c:a libmp3lame`

把音频编码成：

```text
MP3
```

这里不是复制原音频，而是重新编码。

`libmp3lame` 是 FFmpeg 中常用的 MP3 编码器。

## 7. `-b:a 192k`

设置 MP3 音频码率：

```text
192 kbps
```

对于音乐来说属于比较高的质量。

如果设备空间有限，也可以考虑：

```text
128k
```

例如：

```text
-b:a 128k
```

音乐质量仍然不错，文件也会更小。

## 8. 最后的 `"……avi"`

指定输出文件：

```text
Taylor Swift - 22_460_24fps_MJPEG.avi
```

最终输出规格大约为：

| 项目 | 设置 |
|---|---|
| 容器 | AVI |
| 视频编码 | MJPEG |
| 分辨率 | 约 460×342 |
| 帧率 | 24 fps |
| 黑边 | 已裁剪 |
| 视频质量 | `q=8` |
| 音频 | MP3 |
| 音频码率 | 192 kbps |

## 整体处理顺序

```text
原 MP4
↓
裁掉黑边
↓
缩放到 460 宽
↓
转换为 24fps
↓
编码成 MJPEG
↓
音频转 MP3 192kbps
↓
封装成 AVI
```

如果这个 AVI 是专门给单片机或嵌入式播放器使用，还可以进一步固定音频参数，例如：

```text
44.1kHz + 双声道
```

这样通常能提高嵌入式解码端的兼容性和稳定性。
