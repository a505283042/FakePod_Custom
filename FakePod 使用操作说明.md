# FakePod 使用操作说明

FakePod_Full.bin 是集本地音乐播放、歌词与频谱显示、跟随封面自动着色的彩色磁带视觉、分级播放列表、手势控歌、电子书阅读、视频播放、NSF 电子音流、锁屏与息屏显示、TF 卡文件管理以及多种系统设置的新版固件，支持触摸、实体按键和动作手势等多种操作方式。

烧录参数：

| 项目 | 设置 |
|---|---|
| 烧录工具 | flash_download_tool（乐鑫官方固件烧录工具） |
| 文件 | FakePod_Full.bin |
| 地址 | 0x0000 |
| SPI MODE | DIO |
| SPI SPEED | 80MHz |
| Flash Size | 16MB |

# 1. 文件放置

TF 卡中按以下目录放置文件（文件夹名不区分大小写）：

- 音乐：`/MUSIC` —— 支持 MP3、FLAC、WAV（验证支持 FLAC 最高 96kHz），`/MUSIC` 下最多可建二级文件夹。歌曲标签和封面可用 MusicTag 写入，歌词文件建议用 LDDC 下载。
- 电子书：`/txt` —— 放置 `.txt` 文件，可使用子文件夹；请保存为 UTF-8 编码。
- 视频：`/video` —— 放置 AVI 视频，可使用子文件夹。
- 电子音流：`/synth` —— 放置 NSF 文件，可使用子文件夹；NSFE 当前会显示，但暂不能正常播放。

通过“设置 → 连接 → TF卡文件管理”也可以直接连接电脑整理这些文件。

# 2. 两个实体按键

- 电源键短按：由**设置 → 系统 → 辅助键模式**决定——音量模式为 +1，切歌模式为上一首。
- 电源键关机：持续按住直到设备断电；关机过程中会先保存当前使用状态，不要按住一半就松开。
- 辅助键短按：同样由辅助键模式决定——音量模式为 -1，切歌模式为下一首。
- 辅助键长按：屏幕处于锁屏 / AOD 息屏显示 / 全黑熄屏时**直接亮屏并解锁**；音乐 APP 正常亮屏时**打开屏幕操作菜单**，菜单已打开时再按**关闭菜单不执行任何项目**；其它 APP 中不会打开音乐锁屏菜单。

# 3. 屏幕操作菜单

在音乐 APP 中长按辅助键打开，菜单包含：锁屏、AOD 息屏显示、熄屏（全黑）、取消。
- 点击某一行执行该操作；按住后上下移动改变当前选择；在另一行松手不会误执行原来的项目；再次长按辅助键直接取消。
- 锁屏：屏幕继续显示但触摸被锁定，长按辅助键解锁。
- AOD 息屏显示：低亮度音乐信息页并自动锁定，可显示歌曲、歌词、播放进度等；不能直接触摸操作，长按辅助键恢复正常亮屏并解锁。
- 熄屏：屏幕全黑但音乐继续播放，长按辅助键重新亮屏并解锁。

# 4. 音乐主页

主页默认以封面为主要画面，未打开控制层时可直接使用以下手势：
- 点击：轻点中央**打开播放器控制层**，再点控制层空白区域收起；停止操作约 5 秒后自动收起。
- 下拉：从顶部向下拉，**打开音乐列表。**
- 上拉：从底部向上拉，**打开 APP 圆环菜单。**
- 左滑：**进入频谱页。**
- 右滑：**进入歌词页。**
- 向上快速滑：**下一首。**
- 向下快速滑：**上一首。** 已播放超过约 3 秒时第一次先回到本曲开头，再执行才切到前一首（实体键和翻转手势同样适用）。

# 5. 音乐控制层

- 上一首：点击按钮；已播放超过约 3 秒时先回到本曲开头。
- 播放 / 暂停：点击中央播放按钮切换。
- 下一首：点击下一首按钮。
- 拖动进度：按住进度条左右拖动**跳转到指定位置**；当前内容不能跳转时不执行。
- 播放模式：点击图标循环切换**顺序 → 列表循环 → 单曲循环 → 随机 → 顺序**。顺序=播到列表末尾停止；列表循环=末尾后从第一首继续；单曲循环=结束重播本曲；随机=从当前有效列表随机选下一首（“上一首”优先返回本次随机实际听过的上一首）。

# 6. 音量操作

- 控制层调节：先点音量图标进入调节状态，向上拖增大、向下拖减小；再点图标或关闭控制层退出。音量条主要用于显示，不需要直接拖动。
- 静音：点击音量百分比胶囊，**静音 / 取消静音。**

# 7. 封面与磁带模式

控制层下方有“切换到磁带 / 切换到封面”，点击切换主页视觉，播放方式和其它手势不变。
- 磁带页：顶部显示歌曲名和歌手；外壳颜色跟随封面主色自动着色（可在「设置 → 应用 → 音乐播放器 → 磁带配色」切回原装粉色），封面近黑或中性色时用石墨灰壳体，无封面保持默认粉色；底部有同步歌词时显示当前句和下一句，没有则隐藏；播放或拖动进度时卷轴持续转动、走带位置跟随进度，暂停或停止时动画冻结、继续播放后恢复。
- 封面位置调整：画面两侧 ▲ / ▼ 每点一次移动一小步，到安全边界后停止；切换歌曲后新歌曲从默认位置（居中）显示。

# 8. 歌词页

音乐主页右滑进入。
- 左滑返回主页；向上快速滑下一首；向下快速滑上一首；点击页面打开底部播放控制，再次点击或等待一段时间后收起。
- 底部可操作：播放模式、上一首、播放 / 暂停、下一首、音量、播放进度。进度条可拖动跳转；点音量按钮后上下拖调节。有同步歌词时自动显示，没有则显示提示。

# 9. 频谱页

音乐主页左滑进入。
- 右滑返回主页；向上快速滑下一首；向下快速滑上一首；轻点画面循环切换三种频谱样式。
- 可同时显示当前歌曲、歌手、歌词和播放时间等信息。

# 10. 打开音乐列表

在音乐主页从顶部向下拉打开。支持上下拖动浏览、松手后惯性滚动、点击歌曲直接播放、返回键或对应手势返回上一层。列表内容由“播放列表范围”决定。

# 11. 总列表模式

设置：**设置 → 应用 → 播放列表范围 → 总列表**，离开设置 APP 后生效。总列表可在歌曲、歌手、专辑、年代四种分类间左右滑动：
- 歌曲：显示 `/MUSIC` 中全部音乐，点击直接播放。
- 歌手：点击歌手进入该歌手的歌曲列表，再点歌曲播放。
- 专辑：点击专辑进入该专辑的歌曲列表。
- 年代：点击年代进入该年代的歌曲列表。
进入分类详情后，右滑或点击返回回到上一层；在分类中开始播放后，上一首 / 下一首 / 随机都以当前分类为范围。

# 12. 一级列表模式

设置：**设置 → 应用 → 播放列表范围 → 一级列表**，离开设置 APP 后生效。例如 `/MUSIC/我喜欢/` 被选中后，歌曲页显示“我喜欢”下所有歌曲（含子文件夹）。
- 更换一级文件夹：在一级歌曲列表中左滑打开一级文件夹列表，标题栏显示 Music；左边文件夹名、右边歌曲数量，当前使用的有状态标志。点击文件夹切换到该一级文件夹并自动返回歌曲列表；右滑则不更换、返回当前列表。一级列表会记住最近选择的一级文件夹。

# 13. 二级列表模式

设置：**设置 → 应用 → 播放列表范围 → 二级列表**，离开设置 APP 后生效。例如 `/MUSIC/我喜欢/周杰伦/` 只显示该二级文件夹中直接存放的歌曲，不会混入更深一级的歌曲。
- 更换二级文件夹：在二级歌曲列表中左滑打开当前一级目录下的二级文件夹列表（如周杰伦 42、林俊杰 26、陈奕迅 31），左边文件夹名、右边歌曲数量，当前项以小标志表示。点击二级文件夹切换该二级列表并返回歌曲页；右滑直接返回。
- “上一级”：二级文件夹列表第一项，点击进入 `/MUSIC` 下的一级目录选择页（标题栏 Music），只显示真正包含可用二级音乐文件夹的一级目录（如“我喜欢　　6”），左边名称、右边可选的二级文件夹数量。点击一级目录查看其二级文件夹，再点某个二级文件夹即可切换。二级列表独立记忆，不会覆盖一级列表最近使用的文件夹。

# 14. 切换播放列表范围时正在播放的歌曲

从总列表切到一级或二级列表时**不会立即强制换掉当前歌曲**：歌曲属于新列表 → 继续播放并按新位置继续；不属于新列表 → 顺序 / 列表循环模式下一首从新列表第一首开始、上一首从最后一首开始，随机模式下一首直接从新列表随机选择。

# 15. 音乐列表搜索

列表顶部点击搜索按钮进入快速搜索。歌曲、歌手、专辑、一级/二级歌曲列表均可使用底部字母组：ABC、DEF、GHI、JKL、MNO、PQRS、TUV、WXYZ、0-9/#，每点击一组继续缩小范围；已输入条件后最后一个按键变为**退格**，用于删除最后一项。

例如在“歌曲”分类找《七里香》（拼音 QI LI XIANG）：点顶部搜索进入 → 依次点字母组 PQRS（“七”=Q）→ JKL（“里”=L）→ WXYZ（“香”=X），每点一组列表就继续缩小 → 点错可点退格逐组删掉最后一步，再重新选。年代搜索会直接显示年代按钮（1950、1960、1970、…、2020、未知、全部），例如直接点“1990”只看 90 年代的歌。点击结果：歌曲直接播放，歌手 / 专辑 / 年代打开对应内容。返回即退出搜索并回到进入搜索前的列表位置。

# 16. 手势控歌

在**设置 → 应用 → 音乐播放器 → 手势控歌**打开或关闭。屏幕正常亮起、设备已解锁、开关为“开”时生效，适用于音乐 APP 和电子书内。
- 向前翻转：下一首。翻转到位后需迅速回正；长时间保持翻转后再慢慢返回不会切歌。
- 向后翻转：上一首，同样需快速完成翻转并回正。
- 双击设备：播放 / 暂停。
关闭“手势控歌”后，前翻、后翻、双击全部停止控制音乐。

# 17. APP 圆环菜单

音乐主页从屏幕底边向上拉打开。菜单含：音乐、电子音流、拾音频谱、视频、图片播放、电子书、设置，点击可用 APP 即可切换。当前版本实际可用：音乐、电子音流、视频、电子书、设置；“拾音频谱”和“图片播放”仅保留菜单入口，没有可进入的完整功能页。点击圆环外区域关闭菜单；菜单打开时执行页面手势也会先关闭菜单。

# 18. 电子书

进入：**APP 菜单 → 电子书**，读取 `/txt` 文件夹。若「设置 → 应用 → 音乐播放器 → 手势控歌」已打开，电子书内同样可用手势控歌：前翻下一首、后翻上一首、双击播放 / 暂停。
- 文件列表：点击文件夹进入、点击 TXT 打开、上下滑浏览、右滑或点击返回回上一级、根目录返回回 APP 菜单。再次打开阅读过的书会恢复保存的阅读位置。
- 普通阅读模式：打开 TXT 后默认进入带控制项的阅读页面，顶部可返回（回文件列表）、存书签（保存当前位置，保存后显示“已保存”）、全屏（进入全屏阅读）、暂停音乐 / 继续音乐（控制后台普通音乐）。
- 翻页：正文上半部分点击上一页、下半部分点击下一页；底部“上页 / 下页”按钮也可翻页。
- 全屏阅读：点击顶部区域上一页、底部区域下一页；左右滑也可以翻页（左滑下一页、右滑上一页）；长按屏幕退出全屏，恢复带控制项的阅读页面。退出书籍或离开电子书 APP 时保存阅读位置。

# 19. 视频

进入：**APP 菜单 → 视频**，读取 `/video`。提示：用 FakePod_Full AVI 转换工具转换的视频适合本固件播放。
- 视频列表：点击文件夹进入、点击 AVI 开始准备并播放、上下滑浏览、右滑或点击返回回上一层、底部上拉打开 APP 菜单。视频负载较高时播放前可能出现确认页（继续播放 / 取消），按需选择。
- 视频播放中：普通音乐会暂时让位给视频声音；右滑停止当前视频并返回列表。自然播完后，有下一条视频则自动继续，最后一条则返回列表。退出视频播放后恢复之前的普通音乐状态。

# 20. 电子音流

进入：**APP 菜单 → 电子音流**，读取 `/synth`。
- 文件列表：点击文件夹进入、点击 NSF 打开播放页、上下滑浏览、右滑或点击返回回上一级、底部上拉打开 APP 菜单。
- NSF 播放页：底部有播放模式、上一个、播放 / 暂停、下一个、列表。播放模式循环切换**顺序 → 单曲 → 循环 → 顺序**；上一个 / 下一个切换 NSF 内部曲目；播放 / 暂停控制音流；列表或右滑返回 `/synth` 文件浏览页。进入 NSF 播放时普通音乐会暂停，离开后恢复之前的普通音乐状态。
- NSF 专用数字补偿：0~6dB 可调，且输出始终封顶在 0dB。

# 21. 设置 APP

进入：**APP 菜单 → 设置**。主页分为连接、应用、系统、关于，点击进入对应页面。详情页超出屏幕时上下拖动滚动、松手带惯性。右滑或点击返回回上一级；在设置主页再返回回音乐 APP；从底部上拉打开 APP 菜单。

# 22. 设置 → 连接

- USB模式：仅显示当前 USB 工作状态。
- TF卡文件管理：点击进入电脑文件管理状态，可复制 / 删除音乐、新建文件夹、整理 TXT / 视频 / NSF 文件。用完先在电脑上执行**安全弹出 / 安全移除设备**；正常情况下 FakePod 自动重新接管 TF 卡并恢复媒体使用，未自动恢复时可用设备页面的“恢复”操作；传输过程中不要直接拔 TF 卡。退出 TF 管理后 USB 串口号会改变，需用新的串口号烧录固件和查看日志，重启后恢复原串口号。
- BLE模式：当前显示“待接入”，没有可操作功能（留待后续开发）。
- 飞行模式：当前显示“待接入”，没有可操作功能（留待后续开发）。

# 23. 设置 → 应用

- 自启动 APP：当前显示“待接入”。
- 音乐播放器：包含播放列表范围、磁带配色、手势控歌三项，选择后**离开整个设置 APP 后正式生效并保存**。
  - 播放列表范围：点击这一行直接循环**总列表 → 一级列表 → 二级列表 → 总列表**，不进入下一级设置页；具体文件夹回音乐列表通过左滑选择。
  - 磁带配色：点击切换**封面变色 / 原装粉色**——封面变色跟随歌曲封面主色自动着色，原装粉色保持默认粉色壳体。
  - 手势控歌：点击切换**开 / 关**——开：前翻下一首、后翻上一首、双击播放 / 暂停；关：三种动作全部停用。
- 视频播放器：当前显示“待接入”。
- 文本阅读：当前显示“待接入”。
- 电子音流：当前显示“待接入”。
以上“待接入”项目目前没有可执行设置，均留待后续版本开发。

# 24. 设置 → 系统

- 输出模式：点击循环**普通耳机 → 高阻耳机 → 线路输出 → 普通耳机**，选择后立即切换并保存。
- 屏幕亮度：点击亮度行进入调节状态，向上拖提高、向下拖降低，即时显示效果；离开设置 APP 时保存最终亮度，再次点击亮度行退出调节状态。
- 自动熄屏：点击循环**永不 → 10秒 → 30秒 → 1分钟 → 2分钟 → 3分钟 → 永不**，选择后立即生效。
- 息屏显示：点击**开 / 关**。自动熄屏设置了时间时：开 → 超时后进入 AOD；关 → 超时后进入全黑熄屏。
- 辅助键模式：点击切换**音量 / 切歌**——音量模式：电源键短按音量 +1、辅助键短按音量 -1；切歌模式：电源键短按上一首、辅助键短按下一首（仅在音乐 APP 或电子书内生效）。长按辅助键的解锁和屏幕菜单功能不受此选项影响。

# 25. 设置 → 关于

查看设备信息：固件版本、系统版本、当前可用内存、TF 卡容量，仅用于查看，没有点击操作。

# 26. 常用操作速查

| 想做什么 | 操作 |
|---|---|
| 打开播放控制 | 音乐主页轻点中央 |
| 打开音乐列表 | 顶部下拉 |
| 打开 APP 菜单 | 底部上拉 |
| 打开歌词 | 主页右滑 |
| 打开频谱 | 主页左滑 |
| 下一首 | 中央上滑 / 前翻 / 辅助键短按（切歌模式） |
| 上一首 | 中央下滑 / 后翻 / 电源键短按（切歌模式） |
| 播放暂停 | 控制层播放键 / 双击设备 |
| 调音量 | 控制层点音量图标后上下拖 |
| 静音 | 点击音量百分比胶囊 |
| Seek | 拖动进度条 |
| 切磁带/封面 | 控制层底部切换按钮 |
| 一级换文件夹 | 一级歌曲列表左滑 |
| 二级换文件夹 | 二级歌曲列表左滑 |
| 二级换一级父目录 | 二级文件夹页点“上一级” |
| 打开锁屏菜单 | 音乐亮屏时长按辅助键 |
| 从锁屏/AOD/黑屏唤醒 | 长按辅助键 |
| 关机 | 持续按住电源键直到断电 |
| 电脑管理 TF 卡 | 设置 → 连接 → TF卡文件管理 |
