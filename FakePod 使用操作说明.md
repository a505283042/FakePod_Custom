# FakePod 使用操作说明

FakePod_Full.bin 是集本地音乐播放、歌词与频谱显示、跟随封面自动着色的彩色磁带视觉、分级播放列表、手势控歌、电子书阅读、视频播放、NSF 电子音流、锁屏与息屏显示、TF 卡文件管理以及多种系统设置的新版固件，支持触摸、实体按键和动作手势等多种操作方式。

烧录参数：

| 项目 | 设置 |
|---|---|
| 烧录工具 | flash_download_tool（乐鑫官方固件烧录工具） |
| 文件 | FakePod_Full.bin |
| 地址 | 0x0000 |
| SPI MODE | DIO |
| SPI SPEED | 80MHz |
| Flash Size | 16MB |

## 1. 文件放置

TF 卡中按以下目录放置文件：

- 音乐：`/MUSIC`
  - 文件夹名不区分大小写（`/MUSIC` 或 `/music` 均可）。
  - 支持 MP3、FLAC、WAV。
  - 可以自由建立多级文件夹。
- 电子书：`/txt`
  - 文件夹名不区分大小写（`/txt` 或 `/TXT` 均可）。
  - 放置 `.txt` 文件，可使用子文件夹。
  - TXT 请保存为 UTF-8 编码。
- 视频：`/video`
  - 文件夹名不区分大小写（`/video` 或 `/VIDEO` 均可）。
  - 放置 AVI 视频，可使用子文件夹。
- 电子音流：`/synth`
  - 文件夹名不区分大小写（`/synth` 或 `/SYNTH` 均可）。
  - 放置 NSF 文件，可使用子文件夹。
  - NSFE 当前会显示，但暂不能正常播放。

通过“设置 → 连接 → TF卡文件管理”也可以直接连接电脑整理这些文件。

---

# 2. 两个实体按键

设备有“电源键”和“辅助键”。

## 电源键

### 短按

功能由：

**设置 → 系统 → 辅助键模式**

决定。

选择“音量”时：

- 电源键短按：音量 +1。

选择“切歌”时：

- 电源键短按：上一首。

### 关机

持续按住电源键直到设备断电。

关机过程中设备会先保存当前使用状态，因此关机时不要按住一半就松开。

---

## 辅助键

### 短按

同样由“辅助键模式”决定。

选择“音量”时：

- 辅助键短按：音量 -1。

选择“切歌”时：

- 辅助键短按：下一首。

### 长按

在不同屏幕状态下功能不同。

屏幕处于：

- 锁屏
- AOD 息屏显示
- 全黑熄屏

时：

**长按辅助键 → 直接亮屏并解锁。**

在音乐 APP 正常亮屏时：

**长按辅助键 → 打开屏幕操作菜单。**

菜单已经打开时：

**再次长按辅助键 → 关闭菜单，不执行任何项目。**

在其它 APP 正常使用时，长按辅助键不会打开音乐锁屏菜单。

---

# 3. 屏幕操作菜单

在音乐 APP 中长按辅助键打开。

菜单包含：

1. 锁屏
2. AOD 息屏显示
3. 熄屏（全黑）
4. 取消

操作方法：

- 点击某一行：执行该操作。
- 按住后上下移动：改变当前选择。
- 在另一行松手：不会误执行原来的项目。
- 再次长按辅助键：直接取消菜单。

## 锁屏

屏幕继续显示，但触摸操作被锁定。

长按辅助键即可解锁。

## AOD 息屏显示

进入低亮度音乐信息页面，并自动锁定。

可显示当前歌曲、歌词、播放进度等信息。

AOD 下不能直接触摸操作。

长按辅助键恢复正常亮屏并解锁。

## 熄屏

屏幕完全变黑，但音乐可以继续播放。

长按辅助键重新亮屏并解锁。

---

# 4. 音乐主页

开机进入音乐 APP 后，主页默认以封面为主要画面。

没有打开控制层时，可以直接使用以下手势。

## 点击

轻点画面中央区域：

**打开播放器控制层。**

再次点击控制层空白区域：

**收起控制层。**

停止操作约 5 秒后，控制层也会自动收起。

---

## 下拉

从屏幕顶部向下拉：

**打开音乐列表。**

---

## 上拉

从屏幕底部向上拉：

**打开 APP 圆环菜单。**

---

## 左滑

从音乐主页左滑：

**进入频谱页。**

---

## 右滑

从音乐主页右滑：

**进入歌词页。**

---

## 向上快速滑动

在主页中央向上快速滑：

**下一首。**

---

## 向下快速滑动

在主页中央向下快速滑：

**上一首。**

注意：

如果当前歌曲已经播放超过约 3 秒，“上一首”第一次会先回到当前歌曲开头；再执行上一首才会切到前一首。

这个规则同样适用于实体键和翻转手势触发的“上一首”。

---

# 5. 音乐控制层

打开控制层后，可以操作：

## 上一首

点击上一首按钮。

若当前歌曲已播放超过约 3 秒，会先回到本曲开头。

## 播放 / 暂停

点击中央播放按钮切换。

## 下一首

点击下一首按钮。

## 拖动进度

按住播放进度条左右拖动：

**跳转到歌曲指定位置。**

如果当前内容不能跳转，则不会执行。

## 播放模式

点击播放模式图标循环切换：

**顺序 → 列表循环 → 单曲循环 → 随机 → 顺序**

### 顺序

按当前列表依次播放，到列表最后一首后停止。

### 列表循环

播放到列表最后一首后，从第一首继续。

### 单曲循环

当前歌曲结束后重新播放当前歌曲。

### 随机

下一首从当前有效播放列表中随机选择。

“上一首”会优先返回本次随机播放过程中实际听过的上一首。

---

# 6. 音量操作

## 控制层调节音量

先点击控制层中的音量图标。

进入音量调节状态后：

- 向上拖：增大音量。
- 向下拖：减小音量。

再次点击音量图标，或关闭控制层，即退出音量调节状态。

控制层里的音量条主要用于显示，不需要直接拖动音量条。

## 静音

点击显示音量百分比的音量胶囊：

**静音 / 取消静音。**

---

# 7. 封面与磁带模式

控制层下方有：

- 切换到磁带
- 切换到封面

点击即可在两种音乐主页视觉之间切换。

播放方式和其它主页手势不变。

---

## 磁带页

磁带页以磁带画面为主要画面：

- 顶部显示歌曲名和歌手。
- 磁带外壳颜色跟随当前歌曲封面主色自动着色；封面近黑或中性色时使用石墨灰壳体，
  没有封面时保持默认粉色。
- 底部有同步歌词时，显示当前句和下一句歌词；没有歌词时隐藏。
- 播放或拖动进度时，磁带卷轴持续转动、走带位置跟随歌曲进度变化；
  暂停或停止时动画冻结，继续播放后恢复。

---

## 磁带页封面位置调整

磁带画面两侧有封面位置调整按钮：

- ▲：封面向上移动。
- ▼：封面向下移动。

每点一次移动一小步。

移动到安全边界后不会继续移动。

切换歌曲后，新歌曲从默认位置（居中）显示。

---

# 8. 歌词页

音乐主页右滑进入。

## 左滑

返回音乐主页。

## 向上快速滑

下一首。

## 向下快速滑

上一首。

## 点击页面

打开歌词页底部播放控制。

再次点击或等待一段时间后，控制区域收起。

底部可以操作：

- 播放模式
- 上一首
- 播放 / 暂停
- 下一首
- 音量
- 播放进度

进度条可以拖动跳转歌曲。

点击音量按钮后，上下拖动可调节音量。

有同步歌词时自动显示歌词；没有可用歌词时会显示提示。

---

# 9. 频谱页

音乐主页左滑进入。

## 右滑

返回音乐主页。

## 向上快速滑

下一首。

## 向下快速滑

上一首。

## 轻点频谱画面

循环切换三种频谱显示样式。

频谱页同时可以显示当前歌曲、歌手、歌词和播放时间等信息。

---

# 10. 打开音乐列表

在音乐主页从顶部向下拉。

列表支持：

- 上下拖动浏览。
- 松手后继续惯性滚动。
- 点击歌曲直接播放。
- 返回键或对应返回手势返回上一层。

音乐列表的具体内容由“播放列表范围”决定。

---

# 11. 总列表模式

设置：

**设置 → 应用 → 播放列表范围 → 总列表**

离开设置 APP 后生效。

总列表可以在四种分类之间左右滑动：

- 歌曲
- 歌手
- 专辑
- 年代

## 歌曲

显示 `/MUSIC` 中全部音乐。

点击歌曲直接播放。

## 歌手

显示歌手列表。

点击歌手：

→ 进入该歌手的歌曲列表。

再点击歌曲：

→ 播放。

## 专辑

显示专辑列表。

点击专辑：

→ 进入该专辑的歌曲列表。

## 年代

显示年代分类。

点击年代：

→ 进入该年代歌曲列表。

进入歌手、专辑或年代的详情后：

**右滑或点击返回 → 回到上一层。**

在这些分类中开始播放后，上一首、下一首和随机都会以当前分类为范围。

---

# 12. 一级列表模式

设置：

**设置 → 应用 → 播放列表范围 → 一级列表**

离开设置 APP 后生效。

例如：

`/MUSIC/我喜欢/`

被选为当前一级文件夹后，歌曲页会显示：

**“我喜欢”下面的所有歌曲，包括其子文件夹中的歌曲。**

## 更换一级文件夹

在一级歌曲列表中：

**左滑 → 打开一级文件夹列表。**

标题栏显示：

**Music**

列表中：

- 左边：文件夹名。
- 右边：该一级范围的歌曲数量。
- 当前使用的文件夹有状态标志。

点击一个文件夹：

→ 切换到该一级文件夹。  
→ 自动返回歌曲列表。

右滑：

→ 不更换文件夹，返回当前歌曲列表。

一级列表会记住最近选择的一级文件夹。

---

# 13. 二级列表模式

设置：

**设置 → 应用 → 播放列表范围 → 二级列表**

离开设置 APP 后生效。

例如选择：

`/MUSIC/我喜欢/周杰伦/`

歌曲列表只显示：

**“周杰伦”这个二级文件夹中直接存放的歌曲。**

不会继续把它下面更深一级文件夹中的歌曲混进来。

## 更换二级文件夹

在二级歌曲列表中：

**左滑 → 打开当前一级目录下的二级文件夹列表。**

例如当前一级目录是“我喜欢”，可能看到：

- 周杰伦　　　　42
- 林俊杰　　　　26
- 陈奕迅　　　　31

左边是文件夹名，右边是歌曲数量。

当前使用的二级文件夹以小标志表示，不显示“当前”文字。

点击二级文件夹：

→ 切换该二级列表。  
→ 返回歌曲页。

右滑：

→ 返回歌曲页。

---

## 二级列表中的“上一级”

二级文件夹列表第一项为：

**上一级**

点击：

→ 进入 `/MUSIC` 下的一级目录选择页。

标题栏显示：

**Music**

这里只显示真正包含可用二级音乐文件夹的一级目录。

例如：

**我喜欢　　　　6**

左边是一级文件夹名。

右边数字表示该一级目录下可选择的二级文件夹数量。

点击“我喜欢”：

→ 查看“我喜欢”下面的二级文件夹。

再点击某个二级文件夹：

→ 切换到该二级列表。

二级列表会独立记住最近选择的二级文件夹，不会覆盖一级列表最近使用的文件夹。

---

# 14. 切换播放列表范围时正在播放的歌曲

从总列表切到一级或二级列表时：

**不会立即强制换掉当前歌曲。**

如果当前歌曲本身属于新列表：

→ 继续播放，并按它在新列表里的位置继续。

如果当前歌曲不属于新列表：

### 顺序 / 列表循环模式

下一首：

→ 从新列表第一首开始。

上一首：

→ 从新列表最后一首开始。

### 随机模式

下一首：

→ 直接从新列表随机选择一首。

---

# 15. 音乐列表搜索

音乐列表顶部点击搜索按钮进入快速搜索。

在：

- 歌曲
- 歌手
- 专辑
- 一级/二级歌曲列表

中，可以使用底部字母组：

- ABC
- DEF
- GHI
- JKL
- MNO
- PQRS
- TUV
- WXYZ
- 0-9/#

每点击一个字母组，就继续缩小搜索范围。

已经输入搜索条件后，最后一个按键变为：

**退格**

用于删除最后一项搜索条件。

年代搜索会直接显示年代按钮，例如：

- 1950
- 1960
- 1970
- …
- 2020
- 未知
- 全部

点击搜索结果：

- 歌曲 → 直接播放。
- 歌手 / 专辑 / 年代 → 打开对应内容。

返回即可退出搜索，并回到进入搜索前的列表位置。

---

# 16. 手势控歌

在：

**设置 → 系统 → 手势控歌**

可以打开或关闭。

只有在：

- 音乐 APP 位于前台
- 屏幕正常亮起
- 设备已经解锁
- 手势控歌为“开”

时生效。

## 向前翻转

下一首。

翻转到位后需要迅速回到正常握持姿态。

长时间保持翻转后再慢慢返回，不会切歌。

## 向后翻转

上一首。

同样需要快速完成翻转并回正。

## 双击设备

播放 / 暂停。

关闭“手势控歌”后：

- 前翻
- 后翻
- 双击

都会停止控制音乐。

---

# 17. APP 圆环菜单

音乐主页从屏幕底边向上拉打开。

菜单可以看到：

- 音乐
- 电子音流
- 拾音频谱
- 视频
- 图片播放
- 电子书
- 设置

点击可用 APP 即可切换。

当前版本实际可使用：

- 音乐
- 电子音流
- 视频
- 电子书
- 设置

“拾音频谱”和“图片播放”目前仅保留菜单入口，没有可进入的完整功能页。

点击圆环外区域可关闭菜单。

菜单打开时执行页面手势，也会先关闭菜单。

---

# 18. 电子书

进入：

**APP 菜单 → 电子书**

读取 `/txt` 文件夹。

## 文件列表

- 点击文件夹：进入文件夹。
- 点击 TXT：打开电子书。
- 上下滑：浏览文件列表。
- 右滑或点击返回：回到上一级。
- 在根目录返回：回到 APP 菜单。

再次打开阅读过的书，会恢复保存的阅读位置。

---

## 普通阅读模式

打开 TXT 后默认进入带控制项的阅读页面。

顶部可以使用：

### 返回

返回电子书文件列表。

### 存书签

保存当前阅读位置。

保存后按钮会显示“已保存”。

### 全屏

进入全屏阅读。

### 暂停音乐 / 继续音乐

控制后台正在播放的普通音乐。

---

## 翻页

普通阅读模式正文区域：

- 点击正文上半部分：上一页。
- 点击正文下半部分：下一页。

底部的“上页 / 下页”按钮也可以翻页。

---

## 全屏阅读

进入全屏后：

- 点击屏幕顶部区域：上一页。
- 点击屏幕底部区域：下一页。
- 点击屏幕中央：退出全屏，恢复带控制项的阅读页面。

全屏阅读不使用左右滑动翻页。

退出书籍或离开电子书 APP 时，会保存当前阅读位置。

---

# 19. 视频

进入：

**APP 菜单 → 视频**

读取 `/video`。

提示：用 FakePod_Full AVI 转换工具转换的视频适合本固件播放。

## 视频列表

- 点击文件夹：进入。
- 点击 AVI：开始准备并播放。
- 上下滑：浏览列表。
- 右滑或点击返回：回到上一层。
- 从底部上拉：打开 APP 菜单。

如果视频负载较高，播放前可能出现确认页：

- 继续播放
- 取消

按需要选择即可。

---

## 视频播放中

视频播放时，普通音乐会暂时让位给视频声音。

### 右滑

停止当前视频并返回视频列表。

视频自然播放结束后：

- 如果当前文件夹还有下一条视频，会自动继续下一条。
- 如果已经是最后一条，则返回视频列表。

退出视频播放后，之前的普通音乐状态会恢复。

---

# 20. 电子音流

进入：

**APP 菜单 → 电子音流**

读取 `/synth`。

## 文件列表

- 点击文件夹：进入。
- 点击 NSF：打开播放页。
- 上下滑：浏览文件列表。
- 右滑或点击返回：回到上一级。
- 从底部上拉：打开 APP 菜单。

---

## NSF 播放页

底部有：

- 播放模式
- 上一个
- 播放 / 暂停
- 下一个
- 列表

### 播放模式

点击循环切换：

**顺序 → 单曲 → 循环 → 顺序**

### 上一个 / 下一个

如果 NSF 文件中包含多个曲目，用于切换内部曲目。

### 播放 / 暂停

暂停或继续电子音流。

### 列表

返回 `/synth` 文件浏览页。

### 右滑

同样返回文件列表。

进入 NSF 播放时，普通音乐会暂停；离开电子音流播放后，会恢复之前的普通音乐状态。

---

# 21. 设置 APP

进入：

**APP 菜单 → 设置**

主页分为：

- 连接
- 应用
- 系统
- 关于

点击进入对应页面。

详情页内容超过屏幕高度时：

**上下拖动即可滚动，松手后带惯性。**

右滑或点击返回：

→ 返回上一级。

在设置主页再返回：

→ 回到音乐 APP。

从底部上拉：

→ 打开 APP 菜单。

---

# 22. 设置 → 连接

## USB模式

仅显示当前 USB 工作状态。

## TF卡文件管理

点击：

**TF卡文件管理 → 进入**

设备会进入电脑文件管理状态。

电脑识别 TF 卡后，可以：

- 复制音乐。
- 删除音乐。
- 新建文件夹。
- 整理 TXT。
- 整理视频。
- 整理 NSF 文件。

使用完成后，在电脑上先执行：

**安全弹出 / 安全移除设备。**

正常情况下，FakePod 会自动重新接管 TF 卡，并重新恢复媒体使用。

如果没有自动恢复，可以使用设备页面上的“恢复”操作。

文件传输过程中不要直接拔 TF 卡。

## BLE模式

当前显示“待接入”，没有可操作功能。

## 飞行模式

当前显示“待接入”，没有可操作功能。

---

# 23. 设置 → 应用

## 自启动 APP

当前显示“待接入”。

## 播放列表范围

点击这一行直接循环：

**总列表 → 一级列表 → 二级列表 → 总列表**

不进入下一级设置页。

选择后不会立即在设置页面执行。

**离开整个设置 APP 后正式生效并保存。**

具体一级或二级文件夹不在设置里选择，需要回音乐列表通过左滑选择。

## 视频播放器

当前显示“待接入”。

## 文本阅读

当前显示“待接入”。

## 电子音流

当前显示“待接入”。

这些“待接入”项目目前没有可执行设置。

---

# 24. 设置 → 系统

## 输出模式

点击循环：

**普通耳机 → 高阻耳机 → 线路输出 → 普通耳机**

选择后立即切换并保存。

---

## 屏幕亮度

先点击“屏幕亮度”这一行。

进入调节状态后：

- 向上拖：提高亮度。
- 向下拖：降低亮度。

屏幕会立即显示调节效果。

离开设置 APP 时保存最终亮度。

再次点击亮度行可退出调节状态。

---

## 自动熄屏

点击循环：

**永不 → 10秒 → 30秒 → 1分钟 → 2分钟 → 3分钟 → 永不**

选择后立即生效。

---

## 息屏显示

点击：

**开 / 关**

如果“自动熄屏”设置了时间：

- 息屏显示 = 开：超时后进入 AOD。
- 息屏显示 = 关：超时后进入全黑熄屏。

---

## 辅助键模式

点击切换：

**音量 / 切歌**

### 音量模式

- 电源键短按：音量 +1。
- 辅助键短按：音量 -1。

### 切歌模式

- 电源键短按：上一首。
- 辅助键短按：下一首。

长按辅助键的解锁和屏幕菜单功能不受这个选项影响。

---

## 手势控歌

点击：

**开 / 关**

打开后允许：

- 前翻下一首。
- 后翻上一首。
- 双击播放 / 暂停。

关闭后这三种动作全部停用。

---

# 25. 设置 → 关于

这里用于查看设备信息，包括：

- 固件版本
- 系统版本
- 当前可用内存
- TF 卡容量

这些项目仅用于查看，没有点击操作。

---

# 26. 常用操作速查

| 想做什么 | 操作 |
|---|---|
| 打开播放控制 | 音乐主页轻点中央 |
| 打开音乐列表 | 顶部下拉 |
| 打开 APP 菜单 | 底部上拉 |
| 打开歌词 | 主页右滑 |
| 打开频谱 | 主页左滑 |
| 下一首 | 中央上滑 / 前翻 / 辅助键短按（切歌模式） |
| 上一首 | 中央下滑 / 后翻 / 电源键短按（切歌模式） |
| 播放暂停 | 控制层播放键 / 双击设备 |
| 调音量 | 控制层点音量图标后上下拖 |
| 静音 | 点击音量百分比胶囊 |
| Seek | 拖动进度条 |
| 切磁带/封面 | 控制层底部切换按钮 |
| 一级换文件夹 | 一级歌曲列表左滑 |
| 二级换文件夹 | 二级歌曲列表左滑 |
| 二级换一级父目录 | 二级文件夹页点“上一级” |
| 打开锁屏菜单 | 音乐亮屏时长按辅助键 |
| 从锁屏/AOD/黑屏唤醒 | 长按辅助键 |
| 关机 | 持续按住电源键直到断电 |
| 电脑管理 TF 卡 | 设置 → 连接 → TF卡文件管理 |